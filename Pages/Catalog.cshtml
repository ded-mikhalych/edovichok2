@page "/catalog"
@model WebApplication.Pages.CatalogModel
@{
    ViewData["Title"] = "Рецепты | Едовичок";
}

<section class="page-heading">
    <p class="eyebrow">Рецепты</p>
    <h1>Поиск по типу блюда и ключевому ингредиенту без лишних служебных блоков.</h1>
    <p class="page-intro">
        Каталог теперь собирается по двум понятным признакам: к какому типу относится блюдо
        и какой ингредиент в нём играет главную роль.
    </p>
</section>

<section class="catalog-shell">
    <aside class="catalog-sidebar">
        <div class="sidebar-card">
            <p class="card-kicker">Фильтры</p>
            <div id="filtersContainer">
                <label class="field-label">Тип блюда</label>
                <div id="categoriesContainer" class="filter-group"></div>

                <label class="field-label">Ингредиент</label>
                <div id="ingredientsContainer" class="filter-group"></div>

                <div class="filter-actions">
                    <button id="resetBtn" type="button" class="button button-secondary">Сбросить</button>
                    <button id="applyBtn" type="button" class="button button-primary">Применить</button>
                </div>
            </div>
        </div>
    </aside>

    <section class="catalog-stage">
        <div class="catalog-stage-header">
            <div>
                <p class="card-kicker">Живой список</p>
                <h2>Выдача каталога</h2>
            </div>
            <div id="recipesCount" class="recipes-count"></div>
        </div>

        <div class="catalog-search-bar">
            <label class="field-label" for="searchInput">Поиск по названию</label>
            <div class="search-wrapper">
                <input type="text" id="searchInput" placeholder="Введите название блюда..." />
                <ul id="suggestionsList"></ul>
            </div>
        </div>

        <div id="catalogSummary" class="catalog-summary">
            Каталог собирает результаты по текущему поиску и выбранным фильтрам.
        </div>

        <div id="cards" class="catalog-grid"></div>
        <div class="catalog-actions">
            <button id="loadMoreBtn" type="button" class="button button-secondary catalog-load-more" hidden>Показать ещё</button>
        </div>
    </section>
</section>

<div id="recipePreviewModal" class="recipe-preview-modal" hidden>
    <div class="recipe-preview-backdrop" data-close-preview="true"></div>
    <section class="recipe-preview-panel" role="dialog" aria-modal="true" aria-labelledby="recipePreviewTitle">
        <button type="button" class="recipe-preview-close" id="closePreviewBtn" aria-label="Закрыть">×</button>
        <p class="card-kicker" id="recipePreviewCategory">Рецепт</p>
        <h3 id="recipePreviewTitle">Быстрый просмотр</h3>
        <p class="recipe-preview-time" id="recipePreviewTime"></p>
        <p class="recipe-preview-description" id="recipePreviewDescription"></p>
        <div class="recipe-preview-ingredients">
            <p class="recipe-preview-subtitle">Основные ингредиенты</p>
            <ul id="recipePreviewIngredients"></ul>
        </div>
        <a id="recipePreviewLink" class="button button-primary" href="/catalog">Открыть рецепт</a>
    </section>
</div>

@section Scripts {
    <script src="~/js/catalog.js" asp-append-version="true"></script>
}
